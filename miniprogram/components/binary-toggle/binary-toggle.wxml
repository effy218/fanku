<view class="toggle-row">
  <view
    wx:for="{{options}}"
    wx:key="*this"
    class="toggle-opt {{(value === true && index === 0) || (value === false && index === 1) || value === item ? 'on' : ''}}"
    data-index="{{index}}"
    bindtap="onSelect"
  >{{item}}</view>
</view>
